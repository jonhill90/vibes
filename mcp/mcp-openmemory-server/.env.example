# OpenAI API key for LLM and embedding operations
OPENAI_API_KEY=your_openai_api_key_here

# User ID for memory isolation (defaults to 'vibes-user' if not set)
USER=vibes-user

# API URL for the frontend (should point to the MCP server)
NEXT_PUBLIC_API_URL=http://localhost:8765

# User ID for the frontend
NEXT_PUBLIC_USER_ID=vibes-user
