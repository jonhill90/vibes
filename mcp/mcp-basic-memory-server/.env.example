# Host path to your Obsidian vault (or any markdown directory)
HOST_VAULT_PATH=

# Container internal directory (keep this as is)
BASIC_MEMORY_DIR=/obsidian-vault

# Optional: Custom configuration  
# BASIC_MEMORY_CONFIG_PATH=/obsidian-vault/config.json
