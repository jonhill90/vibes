# Vibesbox MCP Server - Environment Configuration

# Path to the vibes directory on the host
# Update this to match your local vibes directory path
VIBES_PATH=/path/to/your/vibes

# MCP server external port
# Internal port is always 8000, external port is configurable to avoid conflicts
# Default: 8053 (avoids conflict with Kong at 8000)
VIBESBOX_PORT=8053

# Optional: Log level for the MCP server
LOG_LEVEL=info
